import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'two-pisys-ppa',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
